spieler:
  name: "MaxMustermann"       # Anzeigename (muss zu status.yaml passen)
  rolle: spieler              # spieler | dm
  # telefon: "+43123456789"   # Signal-Nummer — nur nötig wenn der Signal-Bot genutzt wird
